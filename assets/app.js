const API={trips:"api/trips.php",events:"api/events.php",documents:"api/documents.php",packing:"api/packing.php",budget:"api/budget.php",import:"api/import.php"};
const EVENT_TYPES=["flight","drive","camp","photo","eclipse","car","shopping","food","fuel","hike","poi","reminder","hotel","camping","car-rental","ferry","cruise","workshop","special-event"];
const PACKING_CATEGORIES=["General","Toiletries","Base layer (underwear, wool, thermal)","Mid layer (fleece, sweater)","Outer layer (jacket, shell)","Footwear","Electronics","Documents","Health & medicine","Outdoor gear"];
/** @typedef {{ occurrenceId:string; sourceEventId:string; date:string; time:string|null; occurrenceType:"single"|"start"|"continuation"|"end"; title:string; subtitle?:string; isVirtual:boolean; event?:any; }} TimelineOccurrence */
const PERIOD_EVENT_TYPES=new Set(["hotel","camp","camping","car","car-rental","ferry","cruise","workshop","special-event"]);
const DISPLAY_MODE_OPTIONS=[['single','Kun start og slutt'],['range','Vis hver dag'],['daily','Vis som status i dagshodet']];
const FIELD_SETS={
hotel:[["campName","Hotellnavn","text"],["location","Lokasjon","text"],["booking","Bookingreferanse","text"],["price","Pris","number"]],
camping:[["campName","Campingnavn","text"],["location","Lokasjon / GPS","text"],["booking","Bookingreferanse","text"],["price","Pris","number"],["facilities","Fasiliteter","textarea"]],
"car-rental":[["company","Selskap","text"],["pickupLocation","Henting","text"],["dropoffLocation","Levering","text"],["booking","Bookingreferanse","text"],["carModel","Bilmodell","text"]],
ferry:[["company","Operatør","text"],["fromAirport","Fra","text"],["toAirport","Til","text"],["booking","Bookingreferanse","text"],["seat","Sete","text"]],
cruise:[["company","Skip","text"],["fromAirport","Fra","text"],["toAirport","Til","text"],["booking","Bookingreferanse","text"],["facilities","Fasiliteter","textarea"]],
workshop:[["location","Sted","text"],["booking","Bookingreferanse","text"],["priority","Prioritet","select","Lav|Medium|Høy"],["notes","Notater","textarea"]],
"special-event":[["location","Sted","text"],["booking","Bookingreferanse","text"],["priority","Prioritet","select","Lav|Medium|Høy"],["notes","Notater","textarea"]],

flight:[["flightNo","Flightnummer","text"],["airline","Flyselskap","text"],["fromAirport","Fra flyplass","text"],["toAirport","Til flyplass","text"],["booking","Bookingreferanse","text"],["checkinStatus","Check-in status","select","Planlagt|Check-in åpner|Checked in|Boarding|Fullført"],["seat","Sete","text"],["gate","Gate","text"],["baggage","Bagasje","text"]],
drive:[["startLocation","Fra","text"],["endLocation","Til","text"],["viaLocations","Via / stopp underveis, én per linje","textarea"],["estimatedDriveTime","Estimat kjøretid","text"],["estimatedKm","Estimat km","number"],["routeStatus","Rute-status","select","Planlagt|Bekreftet|Kjørt|Endret"]],
camp:[["campName","Overnatting","text"],["location","Lokasjon / GPS","text"],["booking","Bookingreferanse","text"],["price","Pris","number"],["facilities","Fasiliteter","textarea"]],
car:[["company","Selskap","text"],["pickupLocation","Henting","text"],["dropoffLocation","Levering","text"],["booking","Bookingreferanse","text"],["carModel","Bilmodell","text"]],
photo:[["location","Fotolokasjon","text"],["gps","GPS","text"],["camera","Kamera","text"],["lens","Objektiv","text"],["priority","Prioritet","select","★|★★|★★★|★★★★|★★★★★"],["lightGoal","Lysmål","select","Sunrise|Golden hour|Blue hour|Sunset|Night|Aurora|Cloud drama"],["shotList","Shot list","textarea"]],
eclipse:[["location","Lokasjon","text"],["gps","GPS","text"],["totalityTime","Totalitet tidspunkt","time"],["duration","Varighet","text"],["compassDirection","Kompassretning","text"],["camera1","Camera 1","text"],["camera2","Camera 2","text"],["solarFilter","Solfilter","select","Ja|Nei|Kun av under totalitet"],["weatherBackup","Vær-backup","textarea"]],
shopping:[["store","Butikk","text"],["location","Lokasjon","text"],["shoppingList","Handleliste","textarea"],["estimatedCost","Estimert kostnad","number"]],
food:[["place","Sted","text"],["location","Lokasjon","text"],["mealType","Måltid","select","Frokost|Lunsj|Middag|Snack|Kaffe"],["reservation","Reservasjon","text"]],
fuel:[["station","Stasjon","text"],["location","Lokasjon","text"],["liters","Liter","number"],["pricePerLiter","Pris per liter","number"],["odometer","Kilometerstand","number"],["fuelType","Drivstoff","select","Bensin|Diesel|Lading|Annet"]],
hike:[["trailName","Tur / hike","text"],["startLocation","Startpunkt","text"],["endLocation","Sluttpunkt","text"],["distanceKm","Distanse km","number"],["duration","Varighet","text"],["difficulty","Vanskelighetsgrad","select","Lett|Moderat|Krevende|Hard"],["gear","Utstyr","textarea"]],
poi:[["location","Lokasjon","text"],["category","Kategori","select","Natur|Utsiktspunkt|Kultur|By|Butikk|Annet"],["priority","Prioritet","select","Lav|Medium|Høy|Must see"],["gps","GPS","text"]],
reminder:[["reminderText","Påminnelse","textarea"],["status","Status","select","Åpen|Utført|Utsatt"]]
};
let state={trips:[],currentTripId:null,events:[],documents:[],packing:[],budget:[]};let view="dashboard";let editEventId=null;let draftType="flight";
function icon(t){return{flight:"✈️",drive:"🚗",camp:"⛺",photo:"📷",eclipse:"🌞",food:"🍔",fuel:"⛽",hike:"🥾",poi:"📍",car:"🚙",reminder:"🔔",shopping:"🛒",hotel:"🏨",camping:"🏕️","car-rental":"🚗",ferry:"⛴️",cruise:"🛳️",workshop:"🛠️","special-event":"✨"}[t]||"📌"}
function typeName(t){return{flight:"Fly",drive:"Kjøring",camp:"Overnatting",photo:"Foto",eclipse:"Solformørkelse",food:"Mat",fuel:"Drivstoff",hike:"Tur",poi:"Stopp",car:"Bil",reminder:"Påminnelse",shopping:"Shopping",hotel:"Hotel",camping:"Camping","car-rental":"Bilutleie",ferry:"Ferge",cruise:"Cruise",workshop:"Workshop","special-event":"Spesialhendelse"}[t]||"Event"}
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]||m))}
async function api(url,opts={}){const r=await fetch(url,{credentials:"same-origin",headers:opts.body&&!(opts.body instanceof FormData)?{"Content-Type":"application/json"}:{},...opts});if(r.status===401)location.href="login.php";const data=await r.json().catch(()=>null);if(!r.ok)throw new Error(data?.error||"API error");return data}
async function init(){state.trips=await api(API.trips);state.currentTripId=Number(localStorage.getItem("travel_os_current_trip"))||state.trips[0]?.id||null;if(state.currentTripId)await loadTripData();render()}
async function loadTripData(){if(!state.currentTripId)return;localStorage.setItem("travel_os_current_trip",state.currentTripId);const q=`?trip_id=${state.currentTripId}`;[state.events,state.documents,state.packing,state.budget]=await Promise.all([api(API.events+q),api(API.documents+q),api(API.packing+q),api(API.budget+q)])}
function currentTrip(){return state.trips.find(t=>Number(t.id)===Number(state.currentTripId))}
function header(k,t,s,a=""){return`<div class="topbar"><div><div class="kicker">${k}</div><h2>${t}</h2><p class="subtitle">${s}</p></div><div class="actions">${a}</div></div>`}
function nav(v,i,t){return`<button class="${view===v?"active":""}" data-view="${v}">${i} ${t}</button>`}
function render(){document.getElementById("app").innerHTML=`<div class="app"><aside class="sidebar"><div class="logo"><div class="logo-mark">🧭</div><div><h1>Travel OS v3</h1><p>PHP + MySQL</p></div></div><div class="field"><label>Tur</label><select id="trip-select">${state.trips.map(t=>`<option value="${t.id}" ${Number(t.id)===Number(state.currentTripId)?"selected":""}>${esc(t.name)}</option>`).join("")}</select></div><div class="nav">${nav("dashboard","🏠","Dashboard")}${nav("timeline","📅","Timeline")}${nav("routes","🗺️","Routes")}${nav("documents","📄","Documents")}${nav("packing","🧳","Packing")}${nav("budget","💰","Budget")}${nav("settings","⚙️","Settings")}</div><div class="sidebar-footer"><a href="logout.php">Logg ut</a><br>Data lagres i MySQL. Dokumenter lagres i uploads.</div></aside><main class="main">${content()}</main></div>`;bind()}
function content(){if(!state.currentTripId)return newTripPage();return{dashboard,timeline,routes,documents,packing,budget,settings}[view]()}
function dashboard(){const t=currentTrip();const total=state.budget.reduce((a,b)=>a+Number(b.amount||0),0);const packed=state.packing.filter(p=>Number(p.packed)).length;const pct=state.packing.length?Math.round(packed/state.packing.length*100):0;const nextEvents=[...state.events].sort((a,b)=>String(a.start_date+a.start_time).localeCompare(String(b.start_date+b.start_time))).slice(0,4);const tripSummary=`${t?.destination?`<div><b>Destinasjon:</b> ${esc(t.destination)}</div>`:""}${t?.start_date||t?.end_date?`<div><b>Dato:</b> ${esc([t?.start_date,t?.end_date].filter(Boolean).join(" → "))}</div>`:""}${t?.notes?`<div><b>Notater:</b> ${esc(t.notes)}</div>`:""}`;return`${header("Oversikt",esc(t?.name||"Tur"),`${esc(t?.start_date||"")} → ${esc(t?.end_date||"")}`)}<div class="grid cols-3"><div class="card stat"><div><span class="muted">Events</span><br><strong>${state.events.length}</strong></div><div>📅</div></div><div class="card stat"><div><span class="muted">Dokumenter</span><br><strong>${state.documents.length}</strong></div><div>📄</div></div><div class="card stat"><div><span class="muted">Budsjett</span><br><strong>${total.toLocaleString("no-NO")} kr</strong></div><div>💰</div></div></div><div class="grid cols-2" style="margin-top:18px"><div class="card"><h3>Turoversikt</h3><div class="list">${tripSummary||`<div class="empty">Ingen turinfo lagt inn enda.</div>`}</div></div><div class="card"><h3>Pakkestatus</h3><div class="progress"><span style="width:${pct}%"></span></div><p class="muted">${packed} av ${state.packing.length} pakket.</p></div></div><div class="card" style="margin-top:18px"><h3>Neste hendelser</h3><div class="list">${nextEvents.length?nextEvents.map(e=>`<div class="item"><div><strong>${esc(e.title)}</strong><br><span class="muted">${esc(e.start_date)} ${esc((e.start_time||"").slice(0,5))} · ${esc(typeName(e.event_type))}</span></div></div>`).join(""):`<div class="empty">Ingen planlagte hendelser enda.</div>`}</div></div>`}
function eventForm(){const e=editEventId?state.events.find(x=>Number(x.id)===Number(editEventId)):null;const type=e?.event_type||draftType;const d=e?.data||{};const displayMode=normalizeDisplayMode(type,e?.display_mode);return`<div class="card"><h3>${e?"Rediger event":"Ny event"}</h3><div class="row-4"><div class="field"><label>Dato start</label><input id="ev-start-date" type="date" value="${esc(e?.start_date||currentTrip()?.start_date||"")}"></div><div class="field"><label>Tid start</label><input id="ev-start-time" type="time" value="${esc((e?.start_time||"09:00").slice(0,5))}"></div><div class="field"><label>Type</label><select id="ev-type">${EVENT_TYPES.map(t=>`<option value="${t}" ${type===t?"selected":""}>${icon(t)} ${typeName(t)}</option>`).join("")}</select></div><div class="field"><label>Visning</label><select id="ev-display-mode">${DISPLAY_MODE_OPTIONS.map(([value,label])=>`<option value="${value}" ${displayMode===value?"selected":""}>${esc(label)}</option>`).join("")}</select></div></div><div class="row"><div class="field"><label>Dato slutt</label><input id="ev-end-date" type="date" value="${esc(e?.end_date||"")}"><span class="muted">Brukes spesielt for overnatting/flere dager.</span></div><div class="field"><label>Tid slutt</label><input id="ev-end-time" type="time" value="${esc((e?.end_time||"").slice(0,5))}"></div></div><div class="field"><label>Tittel</label><input id="ev-title" value="${esc(e?.title||"")}"></div><div class="form-section"><div class="form-section-title">${icon(type)} Felter for ${typeName(type)}</div>${renderFields(type,d)}</div><div class="field"><label>Notater</label><textarea id="ev-notes">${esc(e?.notes||"")}</textarea></div><div class="actions"><button id="save-event">${e?"Lagre":"Opprett"}</button>${e?`<button class="secondary" id="cancel-event">Avbryt</button>`:""}</div></div>`}
function renderFields(type,d){const fs=FIELD_SETS[type]||[];let out="";for(let i=0;i<fs.length;i+=2)out+=`<div class="row">${fs.slice(i,i+2).map(f=>field(f,d[f[0]])).join("")}</div>`;return out}
function field([key,label,type,opts],val=""){if(type==="textarea")return`<div class="field"><label>${label}</label><textarea data-field="${key}">${esc(val)}</textarea></div>`;if(type==="select")return`<div class="field"><label>${label}</label><select data-field="${key}">${String(opts||"").split("|").map(o=>`<option value="${esc(o)}" ${String(val)===o?"selected":""}>${esc(o)}</option>`).join("")}</select></div>`;return`<div class="field"><label>${label}</label><input data-field="${key}" type="${type}" value="${esc(val)}"></div>`}
function defaultDisplayMode(type){const t=String(type||"").toLowerCase();if(["hotel","camp","camping","car","car-rental","ferry","cruise"].includes(t))return"daily";if(["workshop","special","special-event"].includes(t))return"range";return"single"}
function normalizeDisplayMode(type,value){const mode=String(value||"").toLowerCase();return["single","range","daily"].includes(mode)?mode:defaultDisplayMode(type)}
function displayModeLabel(mode){return{single:"Kun start og slutt",range:"Vis hver dag",daily:"Vis som status i dagshodet"}[mode]||mode}
function parseDateValue(value){if(!value)return null;const [y,m,d]=String(value).split("-").map(Number);return new Date(Date.UTC(y,m-1,d))}
function addDays(date,days){const d=new Date(date);d.setUTCDate(d.getUTCDate()+days);return d.toISOString().slice(0,10)}
function daysBetween(startDate,endDate){const start=parseDateValue(startDate);const end=parseDateValue(endDate);if(!start||!end)return 0;return Math.round((end-start)/86400000)}
function buildDateRange(startDate,endDate){const days=[];if(!startDate)return[];let cursor=parseDateValue(startDate);const limit=parseDateValue(endDate);if(!cursor||!limit)return[startDate];while(cursor<=limit){days.push(cursor.toISOString().slice(0,10));cursor=new Date(Date.UTC(cursor.getUTCFullYear(),cursor.getUTCMonth(),cursor.getUTCDate()+1));}return days;}
function buildTimelineOccurrences(events){const occurrences=[];[...events].sort((a,b)=>String(a.start_date+a.start_time).localeCompare(String(b.start_date+b.start_time))).forEach(e=>{const sourceEventId=String(e.id);const eventType=String(e.event_type||"").toLowerCase();const displayMode=normalizeDisplayMode(eventType,e.display_mode);const startDate=e.start_date;const endDate=e.end_date;const isMultiDay=Boolean(endDate&&endDate>startDate);if(!isMultiDay||displayMode==="single"){occurrences.push({occurrenceId:`${sourceEventId}-single`,sourceEventId,date:startDate||e.end_date||"",time:e.start_time||null,occurrenceType:"single",title:e.title,isVirtual:false,event:e});return;}const dates=buildDateRange(startDate,endDate);if(displayMode==="daily"){dates.forEach((date,index)=>{const isFirst=index===0;const isLast=index===dates.length-1;const label=isFirst?"Check-in":isLast?"Check-out":"Overnatting";occurrences.push({occurrenceId:`${sourceEventId}-${date}`,sourceEventId,date,time:isFirst?e.start_time||null:isLast?e.end_time||null:null,occurrenceType:"single",title:isFirst||isLast?`${label} – ${e.title}`:e.title,subtitle:dates.length>1?`Dag ${index+1} av ${dates.length}`:undefined,isVirtual:true,event:e});});return;}dates.forEach((date,index)=>{const isFirst=index===0;const isLast=index===dates.length-1;const labels={start:PERIOD_EVENT_TYPES.has(eventType)?"Check-in":"Start",end:PERIOD_EVENT_TYPES.has(eventType)?"Check-out":"Slutt",continuation:PERIOD_EVENT_TYPES.has(eventType)?"Overnatting":"Pågår"};const occurrenceType=isFirst?"start":isLast?"end":"continuation";const subtitle=occurrenceType==="continuation"&&PERIOD_EVENT_TYPES.has(eventType)&&dates.length>1?`Natt ${index} av ${dates.length-1}`:undefined;occurrences.push({occurrenceId:`${sourceEventId}-${date}`,sourceEventId,date,time:isFirst?e.start_time||null:isLast?e.end_time||null:null,occurrenceType,title:isFirst?`${labels.start} – ${e.title}`:isLast?`${labels.end} – ${e.title}`:`${labels.continuation} – ${e.title}`,subtitle,isVirtual:true,event:e});});});return occurrences;}
function timeline(){const occurrences=buildTimelineOccurrences(state.events);const groups={};occurrences.sort((a,b)=>{const aKey=[a.occurrenceType==="continuation"?0:1,a.date,a.time||""].join("|");const bKey=[b.occurrenceType==="continuation"?0:1,b.date,b.time||""].join("|");return aKey.localeCompare(bKey);}).forEach(o=>(groups[o.date]??=[]).push(o));return`${header("Plan","Timeline","Overnatting kan nå ha startdato/tid og sluttdato/tid i samme event.")}${eventForm()}${Object.entries(groups).map(([date,items])=>`<div class="day-heading"><strong>${date}</strong><span>${items.length} elementer</span></div><div class="card">${items.map(occurrenceCard).join("")}</div>`).join("")||`<div class="empty">Ingen events enda.</div>`}`}
function displayMeta(e){return Object.entries(e.data||{}).filter(([k,v])=>String(v??"").trim()).slice(0,10).map(([k,v])=>`<div><b>${esc(k)}:</b> ${esc(String(v).replace(/\n/g," → "))}</div>`).join("")}
function occurrenceCard(o){const e=o.event||{};const docs=state.documents.filter(d=>Number(d.event_id)===Number(e.id));const quiet=o.occurrenceType==="continuation";return`<div class="event${quiet?" event-continuation":""}"><div class="event-time">${esc(o.time?o.time.slice(0,5):quiet?"Heldags":"")}</div><div class="event-body"><div class="event-title"><strong>${icon(e.event_type)} ${esc(o.title)}</strong><span class="badge">${occurrenceTypeLabel(o.occurrenceType)}</span></div><div class="event-meta">${o.subtitle?`<div><b>Detalj:</b> ${esc(o.subtitle)}</div>`:""}${e.end_date?`<div><b>Visning:</b> ${esc(displayModeLabel(normalizeDisplayMode(e.event_type,e.display_mode)))}</div>`:""}${displayMeta(e)}</div>${e.notes?`<p>${esc(e.notes)}</p>`:""}${docs.length?`<p class="muted">📄 ${docs.length} dokument(er)</p>`:""}<div class="actions"><button class="small secondary" data-edit-event="${e.id}">Rediger</button><button class="small secondary" data-doc-event="${e.id}">Last opp dokument</button><button class="small danger" data-delete-event="${e.id}">Slett</button></div></div></div>`}
function occurrenceTypeLabel(type){return{single:"Enkelt",start:"Start",continuation:"Fortsettelse",end:"Slutt"}[type]||type}
function normalizeRouteLocation(value){return String(value??"").replace(/^via\s*:/i,"").trim()}
function splitRouteLocations(value){return String(value??"").split(/[;\n]/).map(x=>normalizeRouteLocation(x)).filter(Boolean)}
function compactRoutePoints(points){const out=[];(points||[]).forEach(point=>{const normalized=normalizeRouteLocation(point);if(!normalized)return;if(out[out.length-1]===normalized)return;out.push(normalized);});return out;}
function mergeRoutePoints(existing,next){const out=[...(existing||[])];const nextPoints=[...(next||[])];if(!nextPoints.length)return out;if(!out.length)return nextPoints;const firstNext=nextPoints[0];if(out[out.length-1]===firstNext)nextPoints.shift();return out.concat(nextPoints.filter(Boolean));}
function buildDriveRoutePoints(event){const d=event.data||{};return compactRoutePoints([normalizeRouteLocation(d.startLocation),...splitRouteLocations(d.viaLocations),normalizeRouteLocation(d.endLocation)]);}
function sortEventsForRoutes(events){return [...events].sort((a,b)=>{const compareDate=String(a.start_date||"").localeCompare(String(b.start_date||""));if(compareDate)return compareDate;const compareTime=String(a.start_time||"").localeCompare(String(b.start_time||""));if(compareTime)return compareTime;return Number(a.sort_order||0)-Number(b.sort_order||0);});}
function segmentRoutePoints(points,maxPointsPerSegment=8){const segments=[];if(!points||points.length<2)return[];for(let i=0;i<points.length;i+=maxPointsPerSegment-1){const segment=points.slice(i,i+maxPointsPerSegment);if(segment.length>=2)segments.push(segment);}return segments;}
function buildRouteSegments(points,travelMode="driving"){return segmentRoutePoints(compactRoutePoints(points)).map((segment,index)=>{const clean=compactRoutePoints(segment);const params=new URLSearchParams({api:"1",origin:clean[0],destination:clean.at(-1),travelmode:travelMode});if(clean.length>2)params.set("waypoints",clean.slice(1,-1).join("|"));return{index:index+1,points:clean,url:`https://www.google.com/maps/dir/?${params.toString()}`};}).filter(segment=>segment.points.length>=2);} 
function buildMapsUrlsForPoints(points,travelMode="driving"){return buildRouteSegments(points,travelMode).map(segment=>segment.url);} 
function mapsTravelMode(type){return{flight:"flight",drive:"driving",car:"driving",hike:"walking",walk:"walking",foot:"walking"}[type]||"driving"}
function mapsUrlFromPoints(points,travelMode="driving"){return buildMapsUrlsForPoints(points,travelMode)[0]||"";}
function mapsUrlForEvent(e){if(e.event_type!=="drive")return"";return mapsUrlFromPoints(buildDriveRoutePoints(e),mapsTravelMode(e.event_type));}
function itineraryPoints(){const points=[];sortEventsForRoutes(itineraryEntries()).forEach(e=>{if(e.event_type!=="drive")return;const pts=buildDriveRoutePoints(e);if(!pts.length)return;if(!points.length){points.push(...pts);return;}points.push(...pts.slice(points[points.length-1]===pts[0]?1:0));});return points;}
function buildRoadTripRoutePlan(events){let points=[];sortEventsForRoutes(events.filter(e=>e.event_type==="drive")).forEach(e=>{points=mergeRoutePoints(points,buildDriveRoutePoints(e));});return compactRoutePoints(points);}
function buildDayRoutePlans(events){const groups={};sortEventsForRoutes(events.filter(e=>e.event_type==="drive")).forEach(e=>{const day=e.start_date||"";if(!day)return;(groups[day]??=[]).push(e);});return Object.entries(groups).map(([day,dayEvents])=>{let points=[];dayEvents.forEach(e=>{points=mergeRoutePoints(points,buildDriveRoutePoints(e));});return{day,events:dayEvents,points:compactRoutePoints(points)};});}
function buildFlightSegments(events){return sortEventsForRoutes(events.filter(e=>e.event_type==="flight")).map(e=>({title:e.title,origin:normalizeRouteLocation(e.data?.fromAirport),destination:normalizeRouteLocation(e.data?.toAirport),routeLabel:`${normalizeRouteLocation(e.data?.fromAirport)} → ${normalizeRouteLocation(e.data?.toAirport)}`,event:e})).filter(x=>x.origin&&x.destination);} 
function routePoints(e){if(e.event_type==="drive")return buildDriveRoutePoints(e);if(e.event_type==="flight")return[normalizeRouteLocation(e.data?.fromAirport),normalizeRouteLocation(e.data?.toAirport)].filter(Boolean);if(e.event_type==="car")return[normalizeRouteLocation(e.data?.pickupLocation),normalizeRouteLocation(e.data?.dropoffLocation)].filter(Boolean);return[];}
function previewRouteDialog(title,points,openHandler){const overlay=document.createElement("div");overlay.style.position="fixed";overlay.style.inset="0";overlay.style.background="rgba(0,0,0,0.7)";overlay.style.display="flex";overlay.style.alignItems="center";overlay.style.justifyContent="center";overlay.style.zIndex="9999";const modal=document.createElement("div");modal.style.background="#fff";modal.style.color="#111";modal.style.width="min(720px,90vw)";modal.style.maxHeight="85vh";modal.style.overflowY="auto";modal.style.borderRadius="12px";modal.style.padding="20px";modal.innerHTML=`<h3>${esc(title)}</h3><p class="muted">Rediger stoppene nedenfor. Tomme linjer blir ignorert.</p><p class="muted" id="route-preview-summary"></p><textarea id="route-preview-editor" style="width:100%;min-height:220px;font-family:monospace;">${esc(compactRoutePoints(points).join("\n"))}</textarea><div class="actions" style="margin-top:12px"><button id="route-preview-open">Åpne i Google Maps</button><button class="secondary" id="route-preview-cancel">Avbryt</button></div>`;overlay.appendChild(modal);document.body.appendChild(overlay);const editor=modal.querySelector("#route-preview-editor");const openButton=modal.querySelector("#route-preview-open");const summary=modal.querySelector("#route-preview-summary");const readPoints=()=>compactRoutePoints(editor.value.split(/\n+/));const refresh=()=>{const edited=readPoints();const segments=buildRouteSegments(edited);openButton.disabled=edited.length<2;summary.textContent=!edited.length?"Legg inn minst to stopp for å åpne Google Maps.":segments.length>1?`Google Maps åpner ${segments.length} nummererte segmenter.`:`Google Maps åpner én kjørerute.`;openButton.textContent=segments.length>1?`Åpne ${segments.length} segmenter i Google Maps`:"Åpne i Google Maps";};refresh();editor.oninput=refresh;openButton.onclick=()=>{const edited=readPoints();if(edited.length<2)return;overlay.remove();openHandler(edited);};modal.querySelector("#route-preview-cancel").onclick=()=>overlay.remove();}
function openRouteSegments(points,travelMode="driving"){buildRouteSegments(points,travelMode).forEach(segment=>window.open(segment.url,"_blank","noopener,noreferrer"));}
function openRoutePreview(title,points,travelMode="driving"){const clean=compactRoutePoints(points);if(clean.length<2)return;previewRouteDialog(title,clean,edited=>openRouteSegments(edited,travelMode));}
function openRoutePreviewForEvent(eventId){const event=state.events.find(e=>Number(e.id)===Number(eventId));if(!event)return;openRoutePreview(`Google Maps – ${event.title}`,buildDriveRoutePoints(event));}
function openRoutePreviewForDay(day){const dayPlan=buildDayRoutePlans(state.events).find(x=>x.day===day);if(!dayPlan)return;openRoutePreview(`Google Maps – ${day}`,dayPlan.points);}
function openRoutePreviewForRoadTrip(){openRoutePreview("Google Maps – komplett roadtrip",buildRoadTripRoutePlan(state.events));}
function formatEventTime(e){const parts=[e.start_date,e.start_time].filter(Boolean);return parts.join(" ").trim()}
function eventSummaryDetails(e){const d=e.data||{};const parts=[];const locationValues=[d.location,d.startLocation,d.endLocation,d.fromAirport,d.toAirport,d.pickupLocation,d.dropoffLocation,d.campName,d.place].filter(Boolean).map(String).map(x=>x.trim()).filter(Boolean);if(locationValues.length)parts.push(`Sted: ${locationValues.join(" → ")}`);const extraEntries=Object.entries(d).filter(([k,v])=>String(v??"").trim() && !["location","startLocation","endLocation","fromAirport","toAirport","pickupLocation","dropoffLocation","campName","place"].includes(k)).slice(0,6);if(extraEntries.length)parts.push(`Detaljer: ${extraEntries.map(([k,v])=>`${k}: ${String(v)}`).join(" • ")}`);if(e.notes)parts.push(`Notater: ${e.notes}`);return parts;}
function itineraryEntries(){return [...state.events].sort((a,b)=>String(a.start_date+a.start_time).localeCompare(String(b.start_date+b.start_time)))}
function itineraryText(){const t=currentTrip();const lines=[`Travel OS - Full itinerary`, `Trip: ${t?.name||""}`, `Destination: ${t?.destination||""}`, `Dates: ${t?.start_date||""} → ${t?.end_date||""}`, "", "Activities:"];itineraryEntries().forEach((e,index)=>{const dateTime=formatEventTime(e);lines.push(`${index+1}. ${dateTime} - ${e.title}`);lines.push(`   Type: ${typeName(e.event_type)}`);const route=routePoints(e).filter(Boolean);if(route.length)lines.push(`   Route: ${route.join(" → ")}`);eventSummaryDetails(e).forEach(detail=>lines.push(`   ${detail}`));lines.push("");});return lines.join("\n").trim();}
function downloadText(filename,content){const blob=new Blob([content],{type:"text/plain;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(url);} 
function packingCategoryLabel(item){return String(item?.category||"").trim()||"General";}
function packingExportText(){const t=currentTrip();const groups={};state.packing.forEach(item=>{const cat=packingCategoryLabel(item);(groups[cat]??=[]).push(item);});const lines=[`Travel OS - Packing list`,`Trip: ${t?.name||""}`,`Destination: ${t?.destination||""}`,`Dates: ${t?.start_date||""} → ${t?.end_date||""}`,"","Categories:"];const categories=Object.keys(groups).sort((a,b)=>a.localeCompare(b));categories.forEach((cat,index)=>{lines.push(`${index+1}. ${cat}`);groups[cat].forEach(item=>{const stateLabel=Number(item.packed)?"x":" ";lines.push(`   - [${stateLabel}] ${item.item_text}${item.place?` (${item.place})`:""}`);});lines.push("");});if(!categories.length)lines.push("No items added yet.");return lines.join("\n").trim();}
function exportPackingList(){downloadText(`${(currentTrip()?.name||"trip").toLowerCase().replace(/[^a-z0-9]+/g,"-")}-packing-list.txt`, packingExportText());}
function tripExportData(){const t=currentTrip();return {trip:t,events:state.events,documents:state.documents,packing:state.packing,budget:state.budget};}
function tripExportJson(){return JSON.stringify(tripExportData(),null,2);} 
function itineraryExportData(){return buildTimelineOccurrences(state.events).map(o=>({date:o.date,time:o.time,occurrenceType:o.occurrenceType,title:o.title,subtitle:o.subtitle,sourceEventId:o.sourceEventId,isVirtual:o.isVirtual}));}
function itineraryMarkdown(){const lines=[`# Travel OS itinerary`, ``, `Trip: ${currentTrip()?.name||""}`, ``, ...itineraryExportData().map(o=>`- ${o.date}${o.time?` ${o.time}`:""} — ${o.title}${o.subtitle?` (${o.subtitle})`:""}`)];return lines.join("\n");}
function itineraryJson(){return JSON.stringify(itineraryExportData(),null,2);}
function exportFullItinerary(){downloadText(`${(currentTrip()?.name||"trip").toLowerCase().replace(/[^a-z0-9]+/g,"-")}-itinerary.txt`, itineraryText());}
function exportFullItineraryMarkdown(){downloadText(`${(currentTrip()?.name||"trip").toLowerCase().replace(/[^a-z0-9]+/g,"-")}-itinerary.md`, itineraryMarkdown());}
function exportFullItineraryJson(){downloadText(`${(currentTrip()?.name||"trip").toLowerCase().replace(/[^a-z0-9]+/g,"-")}-itinerary.json`, itineraryJson());}
function exportTripJson(){downloadText(`${(currentTrip()?.name||"trip").toLowerCase().replace(/[^a-z0-9]+/g,"-")}-trip.json`, tripExportJson());}
function openAllRoutesInGoogleMaps(){openRoutePreviewForRoadTrip();}
function routes(){const itinerary=itineraryEntries();const driveRoutes=sortEventsForRoutes(state.events.filter(e=>e.event_type==="drive")).map(event=>{const points=buildDriveRoutePoints(event);const segments=buildRouteSegments(points);return{event,points,segments};}).filter(route=>route.points.length>=2);const dayPlans=buildDayRoutePlans(state.events).map(plan=>({...plan,segments:buildRouteSegments(plan.points)})).filter(plan=>plan.points.length>=2);const roadTripPoints=buildRoadTripRoutePlan(state.events);const roadTripSegments=buildRouteSegments(roadTripPoints);const flightSegments=buildFlightSegments(state.events);return`${header("Kart","Routes","Google Maps bygges nå fra strukturerte kjørestopp med forhåndsvisning før åpning.")}<div class="card"><div class="actions"><button id="export-full-itinerary" class="small">Eksporter full ruteplan</button><button id="export-full-itinerary-markdown" class="small secondary">Eksporter Markdown</button><button id="export-full-itinerary-json" class="small secondary">Eksporter JSON</button><button id="export-trip-json" class="small secondary">Eksporter hele turen</button><button id="open-all-google-maps" class="small secondary">Forhåndsvis roadtrip</button></div></div><div class="card" style="margin-top:18px"><h3>Komplett roadtrip-rute</h3>${roadTripPoints.length>=2?`<p class="muted">${roadTripPoints.length} stopp · ${roadTripSegments.length||1} segment${roadTripSegments.length===1?"":"er"}</p><div class="list"><div class="item"><div><strong>Drive-events i dato-, tids- og sort_order-rekkefølge</strong><br><span class="muted">${roadTripPoints.map(esc).join(" → ")}</span></div><div class="actions"><button class="small secondary" data-route-preview-roadtrip="1">Forhåndsvis</button></div></div></div>`:`<div class="empty">Ingen komplett roadtrip-rute tilgjengelig ennå.</div>`}</div><div class="card" style="margin-top:18px"><h3>Kjøreruter per dag</h3><div class="list">${dayPlans.length?dayPlans.map(plan=>`<div class="item"><div><strong>${esc(plan.day)}</strong><br><span class="muted">${plan.points.map(esc).join(" → ")}</span><br><span class="muted">${plan.events.length} drive-event${plan.events.length===1?"":"s"} · ${plan.segments.length||1} segment${plan.segments.length===1?"":"er"}</span></div><div class="actions"><button class="small secondary" data-route-preview-day="${plan.day}">Forhåndsvis</button></div></div>`).join(""):`<div class="empty">Ingen dagruter ennå.</div>`}</div></div><div class="card" style="margin-top:18px"><h3>Kjøreruter per drive-event</h3><div class="list">${driveRoutes.length?driveRoutes.map(route=>`<div class="item"><div><strong>${esc(route.event.title)}</strong><br><span class="muted">${esc(formatEventTime(route.event))}</span><br><span class="muted">${route.points.map(esc).join(" → ")}</span><br><span class="muted">${route.segments.length||1} segment${route.segments.length===1?"":"er"}</span></div><div class="actions"><button class="small secondary" data-route-preview-event="${route.event.id}">Forhåndsvis</button></div></div>`).join(""):`<div class="empty">Ingen drive-events med rutedata ennå.</div>`}</div></div><div class="card" style="margin-top:18px"><h3>Flysegmenter</h3><div class="list">${flightSegments.length?flightSegments.map(segment=>`<div class="item"><div><strong>${esc(segment.title)}</strong><br><span class="muted">${esc(formatEventTime(segment.event))}</span><br><span class="muted">${esc(segment.routeLabel)}</span></div></div>`).join(""):`<div class="empty">Ingen flysegmenter ennå.</div>`}</div></div><div class="card" style="margin-top:18px"><h3>Sammlet reiseplan</h3><div class="list">${itinerary.map(e=>`<div class="item"><div><strong>${icon(e.event_type)} ${esc(e.title)}</strong><br><span class="muted">${esc(formatEventTime(e))} · ${esc(typeName(e.event_type))}</span>${routePoints(e).filter(Boolean).length?`<br><span class="muted">Rute: ${esc(routePoints(e).filter(Boolean).join(" → "))}</span>`:""}${eventSummaryDetails(e).map(detail=>`<br><span class="muted">${esc(detail)}</span>`).join("")}</div><div class="actions">${e.event_type==="drive"&&buildDriveRoutePoints(e).length>=2?`<button class="small secondary" data-route-preview-event="${e.id}">Forhåndsvis</button>`:""}</div></div>`).join("")||`<div class="empty">Ingen aktiviteter enda.</div>`}</div></div>`}
function documents(){return`${header("Dokumenter","Documents","Last opp dokumenter og knytt dem til en event.")}<div class="card"><form id="doc-form" class="grid"><div class="row-3"><div class="field"><label>Event</label><select name="event_id"><option value="">Kun tur</option>${state.events.map(e=>`<option value="${e.id}">${esc(e.start_date)} - ${esc(e.title)}</option>`).join("")}</select></div><div class="field"><label>Fil</label><input name="file" type="file" required></div><div class="field"><label>Notat</label><input name="notes"></div></div><button>Last opp</button></form></div><div class="card" style="margin-top:18px"><h3>Opplastede dokumenter</h3><div class="list">${state.documents.map(d=>`<div class="item"><div><strong>${esc(d.original_filename)}</strong><br><span class="muted">${esc(d.notes||"")} ${d.event_id?`· Event #${d.event_id}`:""}</span></div><div class="actions"><a target="_blank" href="${esc(d.file_path)}"><button class="small secondary">Åpne</button></a><button class="small danger" data-delete-doc="${d.id}">Slett</button></div></div>`).join("")||`<div class="empty">Ingen dokumenter.</div>`}</div></div>`}
function packing(){const categoryOptions=PACKING_CATEGORIES.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join("");return`${header("Utstyr","Packing","Pakkeliste lagret i database.")}<div class="card"><div class="row-3"><div class="field"><label>Ting</label><input id="pack-text"></div><div class="field"><label>Kategori</label><select id="pack-category">${categoryOptions}</select></div><div class="field"><label>Hvor</label><input id="pack-place"></div></div><div class="actions"><button id="add-pack">Legg til</button><button id="export-packing-list" class="secondary">Eksporter pakkeliste</button></div><div class="list">${state.packing.map(p=>`<div class="item"><label><input type="checkbox" data-pack="${p.id}" ${Number(p.packed)?"checked":""}> ${esc(p.item_text)} <span class="muted">· ${esc(p.place||"")}</span></label><div class="actions"><span class="badge">${esc(packingCategoryLabel(p))}</span><button class="small danger" data-delete-pack="${p.id}">Slett</button></div></div>`).join("")}</div></div>`}
function budget(){const total=state.budget.reduce((a,b)=>a+Number(b.amount||0),0);return`${header("Budsjett","Budget",`${total.toLocaleString("no-NO")} kr`)}<div class="card"><div class="row-3"><div class="field"><label>Kategori</label><input id="bud-cat"></div><div class="field"><label>Navn</label><input id="bud-name"></div><div class="field"><label>Beløp</label><input id="bud-amount" type="number"></div></div><button id="add-budget">Legg til</button><table class="table"><tbody>${state.budget.map(b=>`<tr><td>${esc(b.category||"")}</td><td>${esc(b.item_name)}</td><td>${Number(b.amount||0).toLocaleString("no-NO")} ${esc(b.currency)}</td><td><button class="small danger" data-delete-budget="${b.id}">Slett</button></td></tr>`).join("")}</tbody></table></div>`}
function newTripPage(){return`${header("Start","Ingen tur valgt","Opprett første tur.")}<div class="card"><div class="field"><label>Navn</label><input id="new-trip-name" value="Island 2026"></div><button id="create-trip">Opprett tur</button></div>`}
function settings(){const t=currentTrip();return`${header("Oppsett","Settings","Turinfo, import og eksport.")}<div class="grid cols-2"><div class="card"><h3>Turinfo</h3><div class="field"><label>Navn</label><input id="trip-name" value="${esc(t?.name||"")}"></div><div class="row"><div class="field"><label>Destinasjon</label><input id="trip-destination" value="${esc(t?.destination||"")}"></div><div></div></div><div class="row"><div class="field"><label>Start</label><input id="trip-start" type="date" value="${esc(t?.start_date||"")}"></div><div class="field"><label>Slutt</label><input id="trip-end" type="date" value="${esc(t?.end_date||"")}"></div></div><div class="field"><label>Notater</label><textarea id="trip-notes">${esc(t?.notes||"")}</textarea></div><div class="actions"><button id="save-trip">Lagre</button><button class="danger" id="delete-trip">Slett tur</button></div></div><div class="card"><h3>Ny tur / import / eksport</h3><div class="field"><label>Ny tur navn</label><input id="new-trip-name"></div><button id="create-trip">Opprett tur</button><hr><div class="field"><label>Importer Travel OS JSON</label><textarea id="import-json"></textarea></div><button id="import-json-btn">Importer som ny tur</button><hr><div class="actions"><button id="export-trip-json-settings" class="secondary">Eksporter hele turen som JSON</button><button id="export-packing-list-settings" class="secondary">Eksporter pakkeliste</button></div></div></div>`}
function bind(){document.querySelectorAll("[data-view]").forEach(b=>b.onclick=()=>{view=b.dataset.view;editEventId=null;render()});const tripSelect=document.getElementById("trip-select");if(tripSelect)tripSelect.onchange=async()=>{state.currentTripId=Number(tripSelect.value);await loadTripData();render()};const type=document.getElementById("ev-type");if(type)type.onchange=()=>{draftType=type.value;if(editEventId){const e=state.events.find(x=>Number(x.id)===Number(editEventId));if(e){e.event_type=draftType;e.data={}}}render()};const saveEvent=document.getElementById("save-event");if(saveEvent)saveEvent.onclick=saveEventHandler;const cancel=document.getElementById("cancel-event");if(cancel)cancel.onclick=()=>{editEventId=null;render()};document.querySelectorAll("[data-edit-event]").forEach(b=>b.onclick=()=>{editEventId=Number(b.dataset.editEvent);const e=state.events.find(x=>Number(x.id)===Number(editEventId));draftType=e?.event_type||"flight";window.scrollTo({top:0,behavior:"smooth"});render()});document.querySelectorAll("[data-delete-event]").forEach(b=>b.onclick=async()=>{if(confirm("Slett event?")){await api(API.events+"?id="+b.dataset.deleteEvent,{method:"DELETE"});await loadTripData();render()}});document.querySelectorAll("[data-doc-event]").forEach(b=>b.onclick=()=>{view="documents";render();setTimeout(()=>{const sel=document.querySelector("[name=event_id]");if(sel)sel.value=b.dataset.docEvent},0)});const docForm=document.getElementById("doc-form");if(docForm)docForm.onsubmit=uploadDoc;document.querySelectorAll("[data-delete-doc]").forEach(b=>b.onclick=async()=>{await api(API.documents+"?id="+b.dataset.deleteDoc,{method:"DELETE"});await loadTripData();render()});const addPack=document.getElementById("add-pack");if(addPack)addPack.onclick=async()=>{await api(API.packing,{method:"POST",body:JSON.stringify({trip_id:state.currentTripId,item_text:v("pack-text"),category:v("pack-category"),place:v("pack-place")})});await loadTripData();render()};const exportPackingBtn=document.getElementById("export-packing-list");if(exportPackingBtn)exportPackingBtn.onclick=exportPackingList;const exportPackingSettingsBtn=document.getElementById("export-packing-list-settings");if(exportPackingSettingsBtn)exportPackingSettingsBtn.onclick=exportPackingList;const exportBtn=document.getElementById("export-full-itinerary");if(exportBtn)exportBtn.onclick=exportFullItinerary;const exportMdBtn=document.getElementById("export-full-itinerary-markdown");if(exportMdBtn)exportMdBtn.onclick=exportFullItineraryMarkdown;const exportJsonBtn=document.getElementById("export-full-itinerary-json");if(exportJsonBtn)exportJsonBtn.onclick=exportFullItineraryJson;const exportTripBtn=document.getElementById("export-trip-json");if(exportTripBtn)exportTripBtn.onclick=exportTripJson;const exportTripSettingsBtn=document.getElementById("export-trip-json-settings");if(exportTripSettingsBtn)exportTripSettingsBtn.onclick=exportTripJson;const openAllMapsBtn=document.getElementById("open-all-google-maps");if(openAllMapsBtn)openAllMapsBtn.onclick=openAllRoutesInGoogleMaps;document.querySelectorAll("[data-route-preview-event]").forEach(b=>b.onclick=()=>openRoutePreviewForEvent(b.dataset.routePreviewEvent));document.querySelectorAll("[data-route-preview-day]").forEach(b=>b.onclick=()=>openRoutePreviewForDay(b.dataset.routePreviewDay));document.querySelectorAll("[data-route-preview-roadtrip]").forEach(b=>b.onclick=openRoutePreviewForRoadTrip);document.querySelectorAll("[data-pack]").forEach(cb=>cb.onchange=async()=>{const p=state.packing.find(x=>Number(x.id)===Number(cb.dataset.pack));if(!p)return;await api(API.packing,{method:"PUT",body:JSON.stringify({...p,packed:cb.checked})});await loadTripData();render()});document.querySelectorAll("[data-delete-pack]").forEach(b=>b.onclick=async()=>{await api(API.packing+"?id="+b.dataset.deletePack,{method:"DELETE"});await loadTripData();render()});const addBudget=document.getElementById("add-budget");if(addBudget)addBudget.onclick=async()=>{await api(API.budget,{method:"POST",body:JSON.stringify({trip_id:state.currentTripId,category:v("bud-cat"),item_name:v("bud-name"),amount:v("bud-amount"),currency:"NOK"})});await loadTripData();render()};document.querySelectorAll("[data-delete-budget]").forEach(b=>b.onclick=async()=>{await api(API.budget+"?id="+b.dataset.deleteBudget,{method:"DELETE"});await loadTripData();render()});const saveTrip=document.getElementById("save-trip");if(saveTrip)saveTrip.onclick=saveTripHandler;const createTrip=document.getElementById("create-trip");if(createTrip)createTrip.onclick=createTripHandler;const delTrip=document.getElementById("delete-trip");if(delTrip)delTrip.onclick=deleteTripHandler;const importBtn=document.getElementById("import-json-btn");if(importBtn)importBtn.onclick=importHandler}
function v(id){return document.getElementById(id)?.value||""}
async function saveEventHandler(){const data={};document.querySelectorAll("[data-field]").forEach(el=>data[el.dataset.field]=el.value);const payload={trip_id:state.currentTripId,id:editEventId,event_type:v("ev-type"),title:v("ev-title")||"Event",start_date:v("ev-start-date"),start_time:v("ev-start-time")||null,end_date:v("ev-end-date")||null,end_time:v("ev-end-time")||null,display_mode:v("ev-display-mode")||normalizeDisplayMode(v("ev-type"),""),notes:v("ev-notes"),data};await api(API.events,{method:editEventId?"PUT":"POST",body:JSON.stringify(payload)});editEventId=null;await loadTripData();render()}
async function uploadDoc(e){e.preventDefault();const fd=new FormData(e.target);fd.append("trip_id",state.currentTripId);await api(API.documents,{method:"POST",body:fd});await loadTripData();render()}
async function saveTripHandler(){const t=currentTrip();await api(API.trips,{method:"PUT",body:JSON.stringify({id:t.id,name:v("trip-name"),destination:v("trip-destination"),start_date:v("trip-start"),end_date:v("trip-end"),notes:v("trip-notes")})});state.trips=await api(API.trips);await loadTripData();render()}
async function createTripHandler(){const name=v("new-trip-name")||"Ny tur";const res=await api(API.trips,{method:"POST",body:JSON.stringify({name})});state.trips=await api(API.trips);state.currentTripId=res.id;await loadTripData();render()}
async function deleteTripHandler(){if(!confirm("Slett hele turen med events og dokumentreferanser?"))return;await api(API.trips+"?id="+state.currentTripId,{method:"DELETE"});state.trips=await api(API.trips);state.currentTripId=state.trips[0]?.id||null;if(state.currentTripId)await loadTripData();render()}
async function importHandler(){const data=JSON.parse(v("import-json"));const res=await api(API.import,{method:"POST",body:JSON.stringify(data)});state.trips=await api(API.trips);state.currentTripId=res.trip_id;await loadTripData();view="timeline";render()}
init().catch(err=>{document.getElementById("app").innerHTML=`<main class="login-card"><h1>Feil</h1><p>${esc(err.message)}</p><p>Sjekk config.php og database.</p></main>`});
